import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'

// Inizializzazione della root di React con StrictMode
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
